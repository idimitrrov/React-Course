import React from "react";
import PropTypes from "prop-types";

const Error = () => {
  return (
    <div className="error">
      <p>Please write something in the search bar!</p>
    </div>
  );
};

export default Error;
